# grcl-idl

`grcl-idl` owns the GRCL IDL grammar, code generation contracts, type support integration points,
and compatibility hashes used across GRCL modules.

This module defines interface and compatibility artifacts only; it does not implement runtime,
backend, or SDK behavior in this skeleton phase.

No runtime behavior is implemented in this skeleton phase.
