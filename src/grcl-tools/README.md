# grcl-tools

`grcl-tools` is the planned module for GRCL CLI, diagnostics, graph inspection, and conformance
tooling.

Its responsibility is to surface contract-aware developer and verification tools around the GRCL
platform modules without implementing runtime, backend, or SDK semantics here.

No runtime behavior is implemented in this skeleton phase.
