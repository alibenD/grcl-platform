# Manifests

This directory will hold repository, compatibility, and release-matrix manifests.

Current planned files:

- `repos.yaml`: external repository references, including the current `github.com/alibenD/grcl`
  implementation repository.
- `compatibility.yaml`: GRCL API, ABI, IDL, wire, SDK, and runtime compatibility matrix.

